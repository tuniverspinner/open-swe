# =============================================================================
# GitHub OAuth Authentication Server - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control

# =============================================================================
# Server Configuration
# =============================================================================

# The port the server will listen on (default: 3001)
PORT=3001

# Environment: development, production, or test (default: development)
NODE_ENV=development

# Frontend URL for CORS and OAuth redirects (default: http://localhost:3000)
FRONTEND_URL=http://localhost:3000

# =============================================================================
# GitHub OAuth Configuration (REQUIRED)
# =============================================================================
# Create a GitHub OAuth App at: https://github.com/settings/applications/new
# Set Authorization callback URL to: http://localhost:3001/auth/github/callback

# GitHub OAuth App Client ID (REQUIRED)
GITHUB_CLIENT_ID=your_github_client_id_here

# GitHub OAuth App Client Secret (REQUIRED)
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# OAuth redirect URI (default: http://localhost:{PORT}/auth/github/callback)
# Only set this if you need to override the default
# GITHUB_REDIRECT_URI=http://localhost:3001/auth/github/callback

# =============================================================================
# JWT Configuration
# =============================================================================

# JWT secret key for signing tokens (REQUIRED - use a strong, random string)
JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_random

# JWT token expiration time (default: 24h)
# Examples: 1h, 30m, 7d, 24h
JWT_EXPIRES_IN=24h

# JWT issuer (default: open-swe-auth)
# JWT_ISSUER=open-swe-auth

# JWT audience (default: open-swe-client)
# JWT_AUDIENCE=open-swe-client

